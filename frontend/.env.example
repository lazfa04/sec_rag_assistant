# Base URL of the FastAPI backend. Copy this file to .env.local and adjust.
# Omit it entirely to fall back to http://localhost:8000.
# Inlined at build time (NEXT_PUBLIC_ prefix), so rebuild after changing it.
NEXT_PUBLIC_API_URL=http://localhost:8000
